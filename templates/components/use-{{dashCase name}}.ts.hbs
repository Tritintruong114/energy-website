import gsap from "gsap"; interface Use{{pascalCase name}}Props { use{{pascalCase
  name
}}?: () => void; } export const use{{pascalCase name}}
= (props: Use{{pascalCase name}}Props) => { const use{{pascalCase name}}
= () => {}; return { use{{pascalCase name}}
}; };